* selective display means that markup presented to the frontend will
  have to be nested
  Or maybe priority display only happens in a chunk?

* [Editor] eventually buffers will have to be abstracted out from the Editor so
  that multiple windows can be looking at the same content

* [Editor] consider storing marks in point coordinates and updating them in .replace
  Consider keeping the point as point coordinates (duh?)
